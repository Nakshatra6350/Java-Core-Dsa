package basics;

public class Primitives { //those which we u cannot divide further data types
    public static void main(String[] args) {
        int rollNo = 64;
        String name = "Nakshatra"; //string is not primitive
        char letter = 'a';
        float marks = 98.8f; // by default all the decimal values are of type double to make sure it is 'float' we add 'f'
        double largeDecimalNumber = 10000000000.000000; // to store larger 'decimal' values
        long largeInteger = 8975653792389032023L; // // to store larger 'integer' values and by default it treated as 'int' so L was appended
        boolean check = true;

    }
}
